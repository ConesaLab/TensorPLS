% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bfm.R
\name{plot_heatmap_components}
\alias{plot_heatmap_components}
\title{Fit\% heatmap (facet by one mode)}
\usage{
plot_heatmap_components(
  tab,
  facet = "R",
  show_values = TRUE,
  outline_best = TRUE
)
}
\arguments{
\item{tab}{A data frame with columns \code{P,Q,R,Fitper,SumComp}.}

\item{facet}{Which mode to facet by: \code{"R"}, \code{"P"}, or \code{"Q"}.}

\item{show_values}{Logical; print Fit\% inside tiles (1 decimal).}

\item{outline_best}{Logical; outline the best tile in each facet.}
}
\value{
A \code{ggplot} object (invisible).
}
\description{
Heatmap of explained variance (Fit\%) over a 2D grid of components while
faceting by the third mode. Draws a bold outline around the best cell
in each facet (ties broken by lower \code{SumComp}).
}
\examples{
\donttest{
# out <- bestfittedmodel(X, 2, 2, 4)
# plot_heatmap_components(out$summary, facet = "R")
}
}
